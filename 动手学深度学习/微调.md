## 微调

```python
import os
import torch
import torchvision
from torch import nn

model = torchvision.models.resnet18(pretrained = True)
model.final_ceng = nn.Linear(input,output)    #修改最后一层
nn.init.Xavier_uniform_(model.final_ceng)

# 优化器配置
optimizer = torch.optim.SGD(
    # 参数组列表（每个字典是一个参数组）
    [
        # 参数组1：params_lx 参数
        {'params': params_lx},
        
        # 参数组2：全连接层参数，单独设置学习率
        {'params': net.fc.parameters(), 'lr': lr * 30}
    ],
    
    # 全局设置（会被没有单独设置的参数组继承）
    lr=lr,              # 基础学习率
    weight_decay=0.001  # L2正则化系数
)   #这段代码是 PyTorch 中为不同参数组设置不同学习率的优化器,配置包含两个参数组，每个参数组有不同的学习率设置
```

从 torchvision 0.13开始，加载预训练模型函数的参数从 pretrained = True 预训练模型参数版本 。就是原来的pretrained_net = torchvision.models.resnet18(pretrained=True) 改成 pretrained_net = torchvision.models.resnet18(weights= torchvision.models.ResNet18_Weights.DEFAULT)