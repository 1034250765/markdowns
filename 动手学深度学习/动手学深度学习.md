## conda环境

conda create -n d2l python=3.8

conda install -y jupyter d2l



## colab 安装d2l

```python
\# Colab 第一格建议写法

!pip install -q -U pip setuptools wheel

!pip install -q requests pandas matplotlib

!pip install -q --no-deps d2l==1.0.3

!pip install -q jedi>=0.16

from d2l import torch as d2l
```







## 数据操作

```python
1.0 标量
[1,2,3]  向量
[[1,2,3],
 [4,5,6],
 [7,8,9]] 矩阵


访问元素
[1,2]
[1,:]一行
[1:2,3]子区域
[::3,::2]
```

L2与L1范数

对元素求和后相加开方

### 梯度

![image-20260319193625529](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260319193625529.png)

```python
import torch

x = torch.arange(4.0)
x

x.requires_grad_(True)  # 等价于x=torch.arange(4.0,requires_grad=True)
x.grad  # 默认值是None

y = 2 * torch.dot(x, x)

y.backward()
x.grad  #y关于向量x的梯度

# 在默认情况下，PyTorch会累积梯度，我们需要清除之前的值
x.grad.zero_()
y = x.sum()
y.backward()
x.grad
```

## torch常用函数

```python
torch.normal(mean=1,std=2,size=(3,4))  #用于从指定均值与方差的正太分布中生成tensor


def init_weights(m):
    if type(m) == nn.Linear:
        nn.init.normal_(m.weight, std=0.01)
net.apply(init_weights)
```

## nn常用函数

```python
net[0].weight.data.normal_(0, 0.01)
net[0].bias.data.fill_(0)
nn.init.normal_(m.weight, std=0.01)也可以
tensor.detach() 会创建一个新的张量，这个张量与原张量共享相同的数值数据，但不会参与梯度计算
tensor.data 是一个属性，它提供了对原张量数据的直接访问，但不创建新的张量对象。分离所得变量requires_grad = False
tensor.item(): 
这个方法用于从单元素张量中提取其Python数值。也就是说，tensor.item() 只适用于标量张量（即包含单个元素的张量）。
net.parameters()
nn.init.normal_(m.weight, std=0.01)
def init_weights(m):
    if type(m) == nn.Linear:
        nn.init.normal_(m.weight, std=0.01)
#net.apply 会遍历模型中的每一个层，并将 init_weights 函数应用到这些层上。
net.apply(init_weights)
```

## softmax回归

**分类问题通常多个输出**

**回归估计一个连续值**

**分类预测一个离散类别**

![image-20260319200625389](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260319200625389.png)

## 激活函数

![image-20260326204211246](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260326204211246.png)

![image-20260326204231541](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260326204231541.png)

![image-20260326204320366](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260326204320366.png)

![image-20260326204335522](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260326204335522.png)

![image-20260326204413124](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260326204413124.png)

![image-20260326204441771](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260326204441771.png)

## 模型选择

训练误差：模型在训练数据集上的误差

泛化误差：模型在新数据上的误差

验证数据集：一个用来评价模型好坏的数据集

测试数据集：只用一次的数据集

K-则交叉验证：在没有足够多的数据时应用

将训练数据分为K块

for i in range（k）

使用第i块作为验证数据集，其余作为训练数据集。

## 过拟合与欠拟合

![image-20260330104907411](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260330104907411.png)

![image-20260330105444693](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260330105444693.png)





## 权重衰退

这个是损失函数

![image-20260402105256371](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260402105256371.png)

**使用均方范数做柔性限制**

![image-20260402105740641](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260402105740641.png)

![image-20260402111105767](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260402111105767.png)

```python
def train_concise(wd):
    net = nn.Sequential(nn.Linear(num_inputs, 1))
    for param in net.parameters():
        param.data.normal_()
    loss = nn.MSELoss(reduction='none')
    num_epochs, lr = 100, 0.003
    # 偏置参数没有衰减
    trainer = torch.optim.SGD([
        {"params":net[0].weight,'weight_decay': wd},
        {"params":net[0].bias}], lr=lr)
    animator = d2l.Animator(xlabel='epochs', ylabel='loss', yscale='log',
                            xlim=[5, num_epochs], legend=['train', 'test'])
    for epoch in range(num_epochs):
        for X, y in train_iter:
            trainer.zero_grad()
            l = loss(net(X), y)
            l.mean().backward()
            trainer.step()
        if (epoch + 1) % 5 == 0:
            animator.add(epoch + 1,
                         (d2l.evaluate_loss(net, train_iter, loss),
                          d2l.evaluate_loss(net, test_iter, loss)))
    print('w的L2范数：', net[0].weight.norm().item())
    //可以在SGD中定义罚
```

## 丢弃法

当我们对图像进行分类时，我们预计向像素添加一些随机噪声应该是基本无影响的。

 具有输入噪声的训练等价于Tikhonov正则化

在层与层之间加入噪音

暂退法在前向传播过程中，计算每一内部层的同时注入噪声，这已经成为训练神经网络的常用技术。 这种方法之所以被称为*暂退法*，因为我们从表面上看是在训练过程中丢弃（drop out）一些

对于x加入噪音，但不改变它的期望。

![image-20260412132137666](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260412132137666.png)

丢弃法将一些输出项 随机置为0来控制模型复杂度

丢弃概率是超参数

## 数组稳定性

![image-20260412204951610](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260412204951610.png)

梯度爆炸的问题

值超出范围

对学习率敏感

学习率太小，训练没有进展。

学习率太大，抖动。

梯度消失-训练无进展。

对于底层网络由为严重，因为反向传播从顶层开始。

使用归一化，可以让梯度 更加稳定，或者梯度剪裁。

合理的初始化和激活函数也有作用。

![image-20260412205727037](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260412205727037.png)

![image-20260412210113663](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260412210113663.png)

## lenet



**查看每一层输出格式的一种方法**

```python
X = torch.rand(size=(1, 1, 28, 28), dtype=torch.float32) 
for layer in net:
    X = layer(X)
    print(layer.__class__.__name__,'output shape: \t',X.shape)
```

![image-20260425172937582](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260425172937582.png)

CNN VISUALIZATION 

![image-20260425173011616](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260425173011616.png)

## ALEXNET

本质上是一个更深更大的LENET

主要改进，丢弃法，MAXPOOLING

## VGG

提出了块的概念

![image-20260425200258467](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260425200258467.png)

## 网络中的网络NIN

卷积层需要较少的参数，但是卷积层后的第一个全连接层的参数很多

参数占用内存和带宽

![image-20260426214909777](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260426214909777.png)

1×1卷积层完成了全连接层和非线性的工作

## 含并行连结的网络 GoogLeNet

几乎超过了一百层，并不是一百层深度。

![image-20260426220747190](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260426220747190.png)

4个路径从不同层面提取信息，然后进行合并。

跟33或者55卷积对比，inception块有更少的参数量





## batch normalization

最上层，最靠近输出层的位置，梯度越大，因为反向传播，越靠近输入层，梯度越小。

上层会很快收敛，下层收敛慢。底层一变动，所有都得跟着变。最后的那些层需要重新学习很多次。导致收敛变慢。

**批量归一化**

固定小批量里面的均值和方差。先将均值方差变为0，1

![image-20260427161103080](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260427161103080.png)

可学习的参数为r和b

作用在卷积和全连接层与激活函数之间

对于全连接层，作用在特征维度。（对每一个全连接做一次数据预处理）

对于卷积层，作用在通道维度（对每一个像素）（样本数是批量大小和高宽）（通道相同位置的像素做归一化）

**批量归一化在做什么**

通过在每个小批量里加入噪音来控制模型复杂度。

可以加速收敛，很少改变精度。



## ResNet

加入更多的层并不一定改变精度。

![image-20260427171858204](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260427171858204.png)

![image-20260427172021181](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260427172021181.png)

残差快使得更深的网络更容易训练

从梯度大小来解释，因为加入了x，所以梯度不会特别小





## 深度学习硬件

提升CPU利用率-提升空间和时间的内存本地性

![image-20260427200705102](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260427200705102.png)

## TPU

DSP用来做shuzi信号处理

功耗低、高性能

可编程阵列，有大量可以编程逻辑单元和可以配置的连接

一次‘编译’需要数个小时

![image-20260428165245805](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260428165245805.png)

## 单机多卡并行

一台机器可以安装多个GPU

模型并行比数据并行难搞

**数据并行**

![image-20260428201108264](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260428201108264.png)

```python
    # 在多个GPU上设置模型
    net = nn.DataParallel(net, device_ids=devices)
    
    
     for epoch in range(num_epochs):
        net.train()
        timer.start()
        for X, y in train_iter:
            trainer.zero_grad()
            X, y = X.to(devices[0]), y.to(devices[0])   //这里DataParallel会自己去拿
            l = loss(net(X), y)
            l.backward()
            trainer.step()
```

你的输入数据会被划分成多个子部分（以下称为副本）送到不同的device中进行计算，而你的模型是在每个device上进行复制一份。

输入的batch是会被平均分到每个device中去，当然你要保证你的batch size大于你的gpu个数。然后在反向传播过程中，每个副本的梯度被累加到原始模块中。

## 数据增广与微调-忘记保存了



## 目标检测

------------------------------------

图片分类用的没有目标检测多

目标检测识别图片里面多个物体的类别和位置

位置通常用边缘框表示

一个边缘框可以通过四个角的数字定义

## 锚框

---------

算法对边框位置的猜测

IOU--交并比

用来表示两个框的相似度

0表示无重合，1表示完全重合。



**以每个像素为中心，生成多个缩放比和宽高比（aspect ratio）不同的边界框。 这些边界框被称为*锚框*（anchor box）。**

假设输入图像的高度为ℎ，宽度为w。 我们以图像的每个像素为中心生成不同形状的锚框：*缩放比*为s∈(0,1]，*宽高比*为r>0。 那么锚框的宽度和高度分别是![hs\sqrt{r}](https://latex.csdn.net/eq?hs%5Csqrt%7Br%7D)和![hs/\sqrt{r}](https://latex.csdn.net/eq?hs/%5Csqrt%7Br%7D)。 要生成多个不同形状的锚框，让我们设置许多缩放比（scale）取值![r_1](https://latex.csdn.net/eq?r_1),…,![r_n](https://latex.csdn.net/eq?r_n)和许多宽高比（aspect ratio）取值![s_1](https://latex.csdn.net/eq?s_1),…,![s_n](https://latex.csdn.net/eq?s_n)。 当使用这些比例和长宽比的所有组合以每个像素为中心时，输入图像将总共有![w*h*n*m](https://latex.csdn.net/eq?w*h*n*m)个锚框。

以同一像素为中心的锚框的数量是![n+m-1](https://latex.csdn.net/eq?n&plus;m-1)。 对于整个输入图像，将共生成![wh(n+m-1)](https://latex.csdn.net/eq?wh%28n&plus;m-1%29)个锚框。

**将真实边界框分配给锚框 **

 在训练集中，我们将每个锚框视为一个训练样本。我们需要每个锚框的*类别*（class）和*偏移量*（offset）标签，其中前者是与锚框相关的对象的类别，后者是真实边界框相对于锚框的偏移量。在预测时，我们为每个图像生成多个锚框，预测所有锚框的类别和偏移量，根据预测的偏移量调整它们的位置以获得预测的边界框，最后只输出符合特定条件的预测边界框。

给定图像，假设锚框是A，真实边界框是B。 让我们定义一个矩阵∈，其中第行、第列的元素是锚框和真实边界框的IoU。 该算法包含以下步骤。

在矩阵中找到最大的元素，并将它的行索引和列索引分别表示为和。然后将真实边界框分配给锚框。这很直观，因为和是所有锚框和真实边界框配对中最相近的。在第一个分配完成后，丢弃矩阵中行和列中的所有元素。

在矩阵中找到剩余元素中最大的元素，并将它的行索引和列索引分别表示为和。我们将真实边界框分配给锚框，并丢弃矩阵中行和列中的所有元素。

此时，矩阵中两行和两列中的元素已被丢弃。我们继续，直到丢弃掉矩阵中列中的所有元素。此时已经为这个锚框各自分配了一个真实边界框。

只遍历剩下的−个锚框。例如，给定任何锚框，在矩阵的第行中找到与的IoU最大的真实边界框，只有当此IoU大于预定义的阈值时，才将分配给。
![image-20260506162418021](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260506162418021.png)



使用非极大值抑制输出

   其基本原理是先在图像中找到所有可能包含目标物体的矩形区域，并按照它们的置信度进行排列。然后从置信度最高的矩形开始，遍历所有的矩形，如果发现当前的矩形与前面任意一个矩形的重叠面积大于一个阈值，则将当前矩形舍去。使得最终保留的预测框数量最少，但同时又能够保证检测的准确性和召回率。

1. 对于每个类别，按照预测框的置信度进行排序，将置信度最高的预测框作为基准。
2. 从剩余的预测框中选择一个与基准框的重叠面积最大的框，如果其重叠面积大于一定的阈值，则将其删除。
3. 对于剩余的预测框，重复步骤2，直到所有的重叠面积都小于阈值，或者没有被删除的框剩余为止。

值得注意的是，NMS的阈值通常需要根据具体的数据集和应用场景进行调整，以兼顾准确性和召回率。



## R-CNN

--------

region besed CNN---基于区域的CNN----使用启发式搜索算法来选择锚框

对于每一个锚框当作一张图片，使用预训练好的模型比如resnet来提取特征。

使用linear来进行分类，使用一个线性回归模型来预测边缘框`偏移`。

**ROL兴趣区域池化**

![image-20260506201256718](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260506201256718.png)

![image-20260506201308726](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260506201308726.png)

变成了同样的大小，之后处理很方便

**Faster-RCNN**

![image-20260506202133268](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260506202133268.png)

不再多次提取特征，而是一次性提取特征，再对锚框做ROL池化

Faster-RCNN使用神经网络代替选择性搜索算法（相对来说还是慢，用的挺多的）

- RCNN是最早，也是最有名的，一类基于锚框的目标检测算法。
- faster-RCNN提升了性能



## 单发多框检测（SSD）

------

- 对于每个像素，生成多个以它为中心的锚框
- 一个基础的网络来抽取特征，然后多个卷积来减半高宽，在每段都生成锚框

![image-20260506203421684](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260506203421684.png)

```PYTHON
def cls_predictor(num_inputs, num_anchors, num_classes):
    return nn.Conv2d(num_inputs, num_anchors * (num_classes + 1),
                     kernel_size=3, padding=1)
    
## num_anchors * (num_classes + 1)对于每一个锚框进行预测，有num_class+1（背景）个输出结果
## 目标检测要做的预测值比较多


def down_sample_blk(in_channels, out_channels):
    blk = []
    for _ in range(2):
        blk.append(nn.Conv2d(in_channels, out_channels,
                             kernel_size=3, padding=1))
        blk.append(nn.BatchNorm2d(out_channels))
        blk.append(nn.ReLU())
        in_channels = out_channels
    blk.append(nn.MaxPool2d(2))
    return nn.Sequential(*blk)

## 使用一个简单的卷积层

def flatten_pred(pred):
    return torch.flatten(pred.permute(0, 2, 3, 1), start_dim=1) ## 通道维度放最后

def concat_preds(preds):
    return torch.cat([flatten_pred(p) for p in preds], dim=1) ##拉平合并

def base_net():
    blk = []
    num_filters = [3, 16, 32, 64]
    for i in range(len(num_filters) - 1):
        blk.append(down_sample_blk(num_filters[i], num_filters[i+1]))
    return nn.Sequential(*blk)

forward(torch.zeros((2, 3, 256, 256)), base_net()).shape

## 基本网络块用于从输入图像中抽取特征。
## 为了计算简洁，我们构造了一个小的基础网络，该网络串联3个高和宽减半块，并逐步将通道数翻倍。


def get_blk(i):
    if i == 0:
        blk = base_net()
    elif i == 1:
        blk = down_sample_blk(64, 128)
    elif i == 4:
        blk = nn.AdaptiveMaxPool2d((1,1))
    else:
        blk = down_sample_blk(128, 128)
    return blk ## 完整网络

def blk_forward(X, blk, size, ratio, cls_predictor, bbox_predictor):
    Y = blk(X)
    anchors = d2l.multibox_prior(Y, sizes=size, ratios=ratio)
    cls_preds = cls_predictor(Y)
    bbox_preds = bbox_predictor(Y)
    return (Y, anchors, cls_preds, bbox_preds)## 前向预测
```











## YOLO

- YOLO将图片均匀分为s×s个锚框

- 每个锚框预测b个真实框





## 语义分割

- 语义分割将每一个像素分类到对应的类别
- 实例分割 目标检测的进化版本
- 语义分割，训练集是图片，标签也是图片
- 增强很麻烦，要考虑标签图片





## 转置卷积

- 卷积通常不会增大输入的高和宽，通常要么不变，要么减半。
- 转置卷积则可以用来增大输入高宽

在同文件夹下有转置卷积的md文件





## 全连接神经网络 FCN

FCN是语义分割奠基性的工作（现在用的不多，思想上比较简单）

![image-20260509201325906](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260509201325906.png)

这里的CNN去掉了最后的全连接层和最后的全局平均池化层

```python
 AdaptiveAvgPool2d(output_size=(1, 1)),
 Linear(in_features=512, out_features=1000, bias=True)]
# 这俩不要了

def bilinear_kernel(in_channels, out_channels, kernel_size):
    factor = (kernel_size + 1) // 2
    if kernel_size % 2 == 1:
        center = factor - 1
    else:
        center = factor - 0.5
    og = (torch.arange(kernel_size).reshape(-1, 1),
          torch.arange(kernel_size).reshape(1, -1))
    filt = (1 - torch.abs(og[0] - center) / factor) * \
           (1 - torch.abs(og[1] - center) / factor)
    weight = torch.zeros((in_channels, out_channels,
                          kernel_size, kernel_size))
    weight[range(in_channels), range(out_channels), :, :] = filt
    return weight
# 用双线性插值初始化核
```



最后将图片还原为224×224

通道数K等于类别数，对每个像素的类别预测





## 样式迁移

将样式图片中的样式迁移到内容图片上，得到合成图片。

![image-20260511155056518](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260511155056518.png)

![image-20260511155158325](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260511155158325.png)

```python
style_layers, content_layers = [0, 5, 10, 19, 28], [25]
# 越小越靠近输入，越大越靠近输出

net = nn.Sequential(*[pretrained_net.features[i] for i in
                      range(max(content_layers + style_layers) + 1)])

#28层之后丢掉
```





## 序列模型



在本质上，音乐、语音、文本和视频都是连续的。

 如果它们的序列被我们重排，那么就会失去原有的意义。

在时间T观察到 $x_t$, 那么得到**T**个不独立的随机变量。

$（x_1,......x_t）~ p(x) $

![image-20260512191650878](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260512191650878.png)

![image-20260512191741934](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260512191741934.png)

![image-20260512192042948](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260512192042948.png)

对前t-1项进行模型建模

对见过的数据建模也叫做自回归模型

![image-20260512200927988](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260512200927988.png)

![image-20260512200941216](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260512200941216.png)

在过去的数据上训练一个MLP

![image-20260512201225052](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260512201225052.png)

![image-20260512201320832](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260512201320832.png)

![image-20260512201331287](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260512201331287.png)

![image-20260512201447237](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260512201447237.png)

在时序模型中，当前数据与之前观察到的数据相关。

自回归模型使用自身过去数据来预测未来。

马尔可夫模型，假设当前数据只跟最近少量数据相关，从而简化模型。

潜变量模型使用潜变量来概括历史信息。





## 文本预处理

将文本当作时序序列来处理。

文本进来后，返回一个整形的向量。

```python
def tokenize(lines, token='word'):  #@save
    """将文本行拆分为单词或字符词元"""
    if token == 'word':
        return [line.split() for line in lines]
    elif token == 'char':
        return [list(line) for line in lines]
    else:
        print('错误：未知词元类型：' + token)

tokens = tokenize(lines)
for i in range(11):
    print(tokens[i])
    
## 将一行变为词元（word或者char）


## 然后将token出现次数做统计（字典），并从大到小排列。
## 最后将token变成一列数字（按出现次数从0到1）
```



## 语言模型



给定文本序列，语言模型的目标是估计联合概率。
$$
p(x_1,x_2,......x_t)
$$
它的应用包括

- 做预训练模型（BERT,GPT-3）
- 给定前面几个词，生成后续文本





假设序列长度为2，我们预测
$$
p(x,x') = p(x)p(x'|x) =  \frac{n(x)}{n}\frac{n(x,x')}{n(x)}
$$
这里n是总词数，n(x)和 n(x,x')  是单个单纯和连续单词的出现次数

很容易扩展到长为3的情况：


$$
p(x,x_2,x_3) = p(x)p(x_2|x)p(x_3|x_2,x)
$$
**当序列很长时，因为文本量不够，很可能n(x1,x2,x3...xn) < 1**

使用马尔可夫假设可以缓解这个问题



一元语法：
$$
p(x1,x2,x3,x4) = p(x1)p(x2)p(x3)p(x4)
$$
二元语法
$$
p(x1,x2,x3,x4) = p(x1)p(x2|x1)p(x3|x2)p(x4|x3)
$$
三元语法
$$
p(x1,x2,x3,x4) = p(x1)p(x2|x1)p(x3|x2,x1)p(x4|x3,x2)
$$


不可能将任意长度的序列都保存下来，更多的时候使用二元语法。



随机采样--每次在原样本上采用一段长为t的序列，它后面那个就是预测值y。 文本预处理不同的是，每个token只用一次。



**如何随机生成一个小批量数据的特征和标签以供读取？**

１、文本序列可以是任意长的，因此任意长的序列可以被划分为具有**相同时间步数**的子序列

２、训练神经网络时，将这些具有相同步数的小批量子序列输入到模型中

- 假设神经网络一次处理具有　ｎ　个时间步的子序列

![img](https://i0.hdslb.com/bfs/note/02463b189b4ee4d30944abbc0d5fe63c2baffc50.png@990w_!web-note.webp)

- 上图展示了从原始文本序列中获得子序列的所有不同的方式，其中　ｎ　等于　５　，并且每个时间步的词元对应于一个字符

３、由于可以选择**任意偏移量**来指示初始位置，因此具有相当大的自由度

- 如果只选择一个偏移量，那么用于训练网络的、所有可能的子序列的覆盖范围将是有限的。因此可以从随机偏移量开始划分序列，来同时获得**覆盖性（ｃｏｖｅｒａｇｅ）**和**随机性（ｒａｎｄｏｍｎｅｓｓ）**





**随机采样（ｒａｎｄｏｍ　ｓａｍｐｌｉｎｇ）**

- 随机采样中，每个样本都是在原始的长序列上任意捕获的子序列
- 在迭代过程中，来自两个**相邻的、随机的、小批量中的**子序列不一定在原始序列上相邻
- 语言建模的目标是**基于当前所看到的词元预测下一个词元**，因此标签是**移位了一个词元**的原始序列





**顺序分区（sequential　ｐａｒｔｉｔｉｏｎｉｎｇ）**

- 顺序分区：迭代过程中，除了对原始序列可以随机抽样外，还可以**保证两个相邻的小批量中的子序列在原始序列上也是相邻的**，这种策略在基于小批量的迭代过程中**保留了拆分的子序列的顺序**







## RNN

![image-20260512201447237](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260512201447237.png)

**潜变量自回归模型** 
$$
x_t 与h_t 和x_t-1相关
$$

$$
ht 和 x_(t-1) h_(t-1) 相关
$$

![image-20260520155317379](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260520155317379.png)

![image-20260520155517428](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260520155517428.png)

去掉第一项就变成了MLP



**衡量语言模型好坏用困惑度**



![image-20260520155959269](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260520155959269.png)

![image-20260520160045008](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260520160045008.png)

## 梯度剪裁

有效预防梯度爆炸

![image-20260520160450693](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260520160450693.png)

![image-20260520160644621](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260520160644621.png)

*更多的RNNS*

应用到语言模型中，循环神经网络根据当前词预测下一次时刻词。

通常使用困惑度（评价交叉熵）来衡量语言模型的好坏





让我们[**首先定义预测函数来生成`prefix`之后的新字符**]，

其中的`prefix`是一个用户提供的包含多个字符的字符串。

在循环遍历`prefix`中的开始字符时，

我们不断地将隐状态传递到下一个时间步，但是不生成任何输出。

这被称为**预热**（warm-up）期，

因为在此期间模型会自我更新（例如，更新隐状态），

但不会进行预测。

预热期结束后，隐状态的值通常比刚开始的初始值更适合预测，

从而预测字符并输出它们。



```PYTHON
def predict_ch8(prefix, num_preds, net, vocab, device):  #@save
    """在prefix后面生成新字符"""
    state = net.begin_state(batch_size=1, device=device)
    outputs = [vocab[prefix[0]]]
    get_input = lambda: torch.tensor([outputs[-1]], device=device).reshape((1, 1))
    for y in prefix[1:]:  # 预热期
        _, state = net(get_input(), state)
        outputs.append(vocab[y])
    for _ in range(num_preds):  # 预测num_preds步
        y, state = net(get_input(), state)
        outputs.append(int(y.argmax(dim=1).reshape(1)))
    return ''.join([vocab.idx_to_token[i] for i in outputs])
```



### RNN简洁实现

 

```python
num_hiddens = 256
rnn_layer = nn.RNN(len(vocab), num_hiddens)

state = torch.zeros((1, batch_size, num_hiddens))
state.shape
```

vocab 是词的总数目，num_hiddens是隐藏状态的大小



```python
X = torch.rand(size=(num_steps, batch_size, len(vocab)))
Y, state_new = rnn_layer(X, state)
Y.shape, state_new.shape

# 输入和隐藏状态

class RNNModel(nn.Module):
    """循环神经网络模型"""
    def __init__(self, rnn_layer, vocab_size, **kwargs):
        super(RNNModel, self).__init__(**kwargs)
        self.rnn = rnn_layer
        self.vocab_size = vocab_size
        self.num_hiddens = self.rnn.hidden_size
        # 如果RNN是双向的（之后将介绍），num_directions应该是2，否则应该是1
        if not self.rnn.bidirectional:
            self.num_directions = 1
            self.linear = nn.Linear(self.num_hiddens, self.vocab_size)
        else:
            self.num_directions = 2
            self.linear = nn.Linear(self.num_hiddens * 2, self.vocab_size)

    def forward(self, inputs, state):
        X = F.one_hot(inputs.T.long(), self.vocab_size)
        X = X.to(torch.float32)
        Y, state = self.rnn(X, state)
        # 全连接层首先将Y的形状改为(时间步数*批量大小,隐藏单元数)
        # 它的输出形状是(时间步数*批量大小,词表大小)。
        output = self.linear(Y.reshape((-1, Y.shape[-1])))
        return output, state
    
    ## 这里自己定义一个输出层
```



## GRU门控循环单元



关注一个序列的时候，不是每个观察值都同样重要。

想要只记住相关的观察需要：

- 更新门
- 遗忘门

![image-20260525200738672](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260525200738672.png)

多了几个可以学习的权重



**候选隐藏状态**

![image-20260525201106345](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260525201106345.png)

这里点是按元素乘。

![image-20260525201408489](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260525201408489.png)

GRU和LSTM除了计算量比rnn大，没有别的坏处。

GRU长度尽量在100以内。







### LSTM长短期记忆网络



忘记门：将值朝向零减少

输入门：决定要不要忽略输入数据

输出门：觉得是不是使用隐藏状态

![image-20260526194218906](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260526194218906.png)

![image-20260526194340586](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260526194340586.png)

![image-20260526194422113](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260526194422113.png)

![image-20260526194553665](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260526194553665.png)



## 深度循环神经网络

`如何得到更多的非线性` 多加几个隐藏层

![image-20260529154425959](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260529154425959.png)



深度循环神经网络使用多个隐藏层来获得非线性性。

```python
lstm = nn.LSTM(num_inputs,num_hiddens,num_layers)
```





## 双向循环神经网络

![image-20260529201528461](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260529201528461.png)

![image-20260529201617083](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260529201617083.png)

一个前向RNN隐藏层

一个反向RNN隐藏层

实现起来也很简单

把序列反过来加入一个rnn

![image-20260529201851615](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260529201851615.png)

![image-20260529201916237](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260529201916237.png)

![image-20260529202006523](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260529202006523.png)

![image-20260529202015570](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260529202015570.png)

双向RNN不能用于推理



**如何使用**

```python
lstm = nn.LSTM(num_inputs,num_hiddens,num_layers,bidirectional=True)
```

## 机器翻译数据集

```python
def tokenize_nmt(text, num_examples=None):
    """词元化“英语－法语”数据数据集"""
    source, target = [], []
    for i, line in enumerate(text.split('\n')):
        if num_examples and i > num_examples:
            break
        parts = line.split('\t')
        if len(parts) == 2:
            source.append(parts[0].split(' '))
            target.append(parts[1].split(' '))
    return source, target

# 词源化

src_vocab = d2l.Vocab(source, min_freq=2,
                      reserved_tokens=['<pad>', '<bos>', '<eos>'])
len(src_vocab)

#返回词汇表

#@save
def truncate_pad(line, num_steps, padding_token):
    """截断或填充文本序列"""
    if len(line) > num_steps:
        return line[:num_steps]  # 截断
    return line + [padding_token] * (num_steps - len(line))  # 填充

truncate_pad(src_vocab[source[0]], 10, src_vocab['<pad>'])

#固定句子长度

#@save
def load_data_nmt(batch_size, num_steps, num_examples=600):
    """返回翻译数据集的迭代器和词表"""
    text = preprocess_nmt(read_data_nmt())
    source, target = tokenize_nmt(text, num_examples)
    src_vocab = d2l.Vocab(source, min_freq=2,
                          reserved_tokens=['<pad>', '<bos>', '<eos>'])
    tgt_vocab = d2l.Vocab(target, min_freq=2,
                          reserved_tokens=['<pad>', '<bos>', '<eos>'])
    src_array, src_valid_len = build_array_nmt(source, src_vocab, num_steps)
    tgt_array, tgt_valid_len = build_array_nmt(target, tgt_vocab, num_steps)
    data_arrays = (src_array, src_valid_len, tgt_array, tgt_valid_len)
    data_iter = d2l.load_array(data_arrays, batch_size)
    return data_iter, src_vocab, tgt_vocab

#返回 句子长度，句子，原句子长度，原句子，两个vocab
```



## 编码器解码器

![image-20260601145536094](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260601145536094.png)

![image-20260601150448855](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260601150448855.png)

**这里decode也处理了一些额外的输入**

```python
from torch import nn


#@save
class Encoder(nn.Module):
    """编码器-解码器架构的基本编码器接口"""
    def __init__(self, **kwargs):
        super(Encoder, self).__init__(**kwargs)

    def forward(self, X, *args):
        raise NotImplementedError
        
#endocer 就是一个简单的mudule


#@save
class Decoder(nn.Module):
    """编码器-解码器架构的基本解码器接口"""
    def __init__(self, **kwargs):
        super(Decoder, self).__init__(**kwargs)

    def init_state(self, enc_outputs, *args):
        raise NotImplementedError

    def forward(self, X, state):
        raise NotImplementedError
        
        
#解码器需要init_state用于将编码器的输出（`enc_outputs`）转换为编码后的状态。
#注意，此步骤可能需要额外的输入，例如：输入序列的有效长度，
#@save
class EncoderDecoder(nn.Module):
    """编码器-解码器架构的基类"""
    def __init__(self, encoder, decoder, **kwargs):
        super(EncoderDecoder, self).__init__(**kwargs)
        self.encoder = encoder
        self.decoder = decoder

    def forward(self, enc_X, dec_X, *args):
        enc_outputs = self.encoder(enc_X, *args)
        dec_state = self.decoder.init_state(enc_outputs, *args)
        return self.decoder(dec_X, dec_state)
    
    #整合encoder decoder        
```



## seq2seq

最早用于机器翻译

给定一个原句子自动翻译为目标语言

![image-20260601152048526](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260601152048526.png)

这里双向RNN可以做encoder，读取输入句子。

解码器使用另一个单向RNN来输出。

![image-20260601153115559](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260601153115559.png)

编码器是没有输出的RNN

编码器**最后时间步**的隐藏状态，用作解码器的**初始**隐藏状态。

- **训练时**解码器使用目标句子作为输入。（就算翻译错了，也没事。）
- **推理时**没有正确的句子，只能使用上一时刻的输出。

![image-20260601153915615](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260601153915615.png)

![image-20260601153930149](C:\Users\LYX10\AppData\Roaming\Typora\typora-user-images\image-20260601153930149.png)
