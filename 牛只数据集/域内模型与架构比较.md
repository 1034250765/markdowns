# 域内模型与架构比较（服务器核查记录）

> 核查日期：2026-09-02。对象：文章初稿 4.3 节四种架构（YOLO11m、RT-DETR-L、Faster R-CNN R50-FPN、RetinaNet R50-FPN）的 checkpoint 选择规则、输入尺寸、训练预算、硬件和软件环境、AMP/batch/环境差异，以及 Precision / Recall / F1。
>
> 所有结论均来自服务器 `211.69.141.71:55522` 的 `/data1/yxli/CODE/ultralytics/self_mAP/` 下的 `args.yaml`、`effective_config.py`、训练日志与脚本，证据路径见文末。本笔记为核查记录，文章初稿正文未改动。

## 1. 训练配置

### 1.1 总表

| 项目 | YOLO11m | RT-DETR-L | Faster R-CNN R50-FPN | RetinaNet R50-FPN |
|---|---|---|---|---|
| 框架 | Ultralytics 8.4.92 | Ultralytics 8.4.92 | MMDetection 3.3.0 | MMDetection 3.3.0 |
| 基础配置 | Ultralytics 默认 detect | Ultralytics 默认（RTDETR 类） | 官方 `faster-rcnn_r50_fpn_1x_coco.py` | 官方 `retinanet_r50_fpn_1x_coco.py` |
| 预训练权重 | `yolo11m.pt`（COCO，39M） | `rtdetr-l.pt`（COCO，64M） | COCO 官方 1x `..._20200130-047c8118.pth` | COCO 官方 1x `..._20200130-c2398f9e.pth` |
| 训练预算 | 100 epochs（patience=100，无早停） | 100 epochs（patience=100，无早停） | 100 epochs（max_epochs=100，val_interval=1） | 100 epochs（val_interval=1） |
| 名义 batch | 16（首批 3 个）→ 32（其余 11 个） | 12（全部 14 个） | 有效 batch 16（见 1.4） | 有效 batch 16（micro 2 × 累积 8） |
| 有效优化 batch | 64（nbs=64 自动累积） | 60（nbs=64，accumulate=5） | 16（无 nbs 概念） | 16 |
| 输入尺寸 | 640×640（letterbox） | 640×640 | Resize `scale=(1333, 800)` keep_ratio（训练/测试一致） | 同左 |
| AMP | **开** | **关**（脚本 `--no-amp`） | **关**（普通 OptimWrapper） | **关** |
| 优化器（实际） | AdamW(lr=0.002, momentum=0.9)，auto 自动选择 | MuSGD(lr=0.01, momentum=0.9)，auto 自动选择 | SGD(lr=0.02, momentum=0.9, wd=1e-4) | SGD(lr=0.01, momentum=0.9, wd=1e-4) |
| LR 调度 | Ultralytics 默认：warmup 3 ep + 线性衰减（cos_lr=False，close_mosaic=10） | 同左 | LinearLR warmup 500 iter + MultiStepLR γ=0.1 @ [67, 92]（100 轮的 2/3 与 11/12） | 同左 |
| checkpoint 规则 | 见 1.2 | 见 1.2 | 见 1.2 | 见 1.2 |
| 随机性 | seed=0, deterministic=True | seed=0 | seed=0, deterministic=False, cudnn_benchmark=True | seed=0 |
| 训练时间 | 2026-07-11 ~ 07-16 | 2026-07-17 ~ 07-18 | 2026-07-25 ~ 07-28 | 2026-08-04 ~ 08-06 |

### 1.2 checkpoint 选择规则

**Ultralytics（YOLO11m、RT-DETR-L）**

- 框架自动保存 `best.pt` / `last.pt`：`best.pt` 按综合 fitness = 0.1×mAP50 + 0.9×mAP50-95 保存；
- 文章 4.3 表格口径：**"最佳轮次"取 `results.csv` 中验证集 mAP@0.5:0.95 的最高行**，该行的 mAP、P、R 为同一轮权重（已验证：fitness-best 轮与 mAP 最高轮可能不同，例如两者在部分 run 上不重合，文章 225 行已声明该区别）；
- 本笔记第 3 节的 P/R/F1 即取自该行（口径与表格 mAP 完全一致）；
- 已实证：`results.csv` 某行的 P/R 与独立 `model.val()` 重评几乎一致（yolo11s/8-calves 第 53 轮：P 0.9517/0.9520、R 0.9403/0.9412、mAP50-95 0.5534/0.5530），差异为推理数值噪声。

**MMDetection（Faster R-CNN、RetinaNet）**

- `CheckpointHook`：`interval=1`（每轮保存）、`max_keep_ckpts=3`、**`save_best='coco/bbox_mAP'`（rule=greater）**；
- 产出 `best_coco_bbox_mAP_epoch_{XX}.pth`，XX 即该 run 中 `coco/bbox_mAP`（= mAP50-95 平均）最高的轮次；
- 每轮验证（`val_interval=1`），最佳轮与保存的 .pth 严格对应（已用 28 份评测的 scalars 对照核验，14/14×2 与文章表格 mAP 一致）。

### 1.3 输入尺寸

- Ultralytics：`imgsz=640`，letterbox 到 640×640（`rect=False`）；
- MMDetection：`Resize(scale=(1333, 800), keep_ratio=True)`，保持宽高比的长边 1333 / 短边 800，训练与测试 pipeline 一致；
- 两框架输入面积相差约 4 倍，这是架构比较中最主要的不可比因素之一（文章 209/223/330 行均已提示）。

### 1.4 训练预算与 batch 细节

- **YOLO11m**：全部 14 个 run 均 100 epochs。`animals_10`、`CBPD_ODD`、`HCRD`（首批，2026-07-11）batch=16；其余 11 个（含 COCO/NWAFU/CImage 单类修正重训、8-calves/COLO/MooTrack360/XGain 新增、Cows2021/diarycow/CID/Google）batch=32。
  - 注意 Ultralytics 的 `nbs=64`：优化器按名义 batch 64 归一，batch=16 时内部梯度累积 4 次、batch=32 时累积 2 次，**有效优化 batch 恒为 64**——这是 batch 16→32 对结果影响有限的机制原因。
- **RT-DETR-L**：全部 14 个 run 均 batch=12、`nbs=64` → accumulate=5，有效优化 batch 60。Cows2021 为从中断的 `last.pt` 断点续训完成（args.yaml 的 model 字段可证）。
- **Faster R-CNN**：目录 `100ep_bs16`。有效 batch 16 有两种实现：
  - 前 6 个（8-calves、animals_10、CBPD_ODD、CID、CImage、COCO，2026-07-25 单独启动）：micro-batch 16、无梯度累积；
  - 后 8 个（COLO、Cows2021、diarycow、Google Open Image、HCRD、MooTrack360、NWAFU_CD、XGain，由 `run_faster_rcnn_all.sh` 批量跑）：micro-batch 2 × 梯度累积 8（显存受限的等价实现）；
  - 两种实现的 lr 均为 SGD 0.02（官方 1x 基准 lr 对应 batch 16，`auto_scale_lr` 禁用、手动线性缩放，两种实现下数值相同）。
- **RetinaNet**：目录 `100ep_ebs16`（ebs=effective batch size）。全部 14 个由 `run_retinanet_all.sh` 以 micro-batch 2 × 累积 8 训练，lr=0.01（RetinaNet 官方基准）。
- 更新步数提示：各架构每 epoch 的优化器更新步数不同（有效 batch 64/60/16/16），若文章按第 665 行要求"报告实际更新步数"，需按各数据集训练图数量分别计算。

### 1.5 AMP

| 模型 | AMP | 证据 |
|---|---|---|
| YOLO11m | True（全部 14 个） | 各 `args.yaml` `amp: true`，日志 `amp=True` |
| RT-DETR-L | False（全部 14 个） | `run_rtdetr_all.sh` 显式 `--no-amp`，`args.yaml` `amp: false` |
| Faster R-CNN | False | `run_faster_rcnn_all.sh` 默认 `AMP=0`；effective_config 的 optim_wrapper 为普通 `OptimWrapper`（非 `AmpOptimWrapper`） |
| RetinaNet | False | 同上 |

### 1.6 优化器实际选择（重要：均非配置显式指定）

- YOLO11m / RT-DETR-L 传 `optimizer=auto`，日志明确打印"ignoring lr0=0.01 and momentum=0.937 and determining best optimizer automatically"：
  - YOLO11m 实际：**AdamW(lr=0.002, momentum=0.9)**，参数组 weight decay 0.0005（nc=1 时 auto 的 lr_fit=0.002）；
  - RT-DETR-L 实际：**MuSGD(lr=0.01, momentum=0.9)**（Ultralytics 8.4.92 为 transformer 检测器选择的 μP-SGD）；
- MMDet 两模型用各自官方 1x 基准：Faster R-CNN SGD 0.02、RetinaNet SGD 0.01，momentum 0.9、wd 1e-4，MultiStepLR 里程碑 [67, 92]。

### 1.7 数据增强与随机性

- Ultralytics（两模型同，默认）：mosaic 1.0（close_mosaic 10）、HSV h/s/v 0.015/0.7/0.4、translate 0.1、scale 0.5、fliplr 0.5、erasing 0.4、mixup 0、copy_paste 0、workers 8；
- MMDetection（官方 1x 管线，两模型同）：`LoadImageFromFile → LoadAnnotations → Resize(1333,800,keep_ratio) → RandomFlip(prob=0.5) → PackDetInputs`，无其他增强；
- seed 全部为 0；Ultralytics deterministic=True，MMDet deterministic=False 且 cudnn_benchmark=True。

## 2. 硬件与软件环境

### 2.1 硬件

- **NVIDIA A100-PCIE-40GB**（可用显存 40338 MiB），驱动 545.23.06；
- 全部 4 个架构、全部 28 个域内 run 均在**同一块卡、单卡**（`device=0`）训练。

### 2.2 软件环境（三个）

| 环境 | 服务对象 | Python | PyTorch | CUDA | 其他 |
|---|---|---|---|---|---|
| conda `ultralytics` | YOLO11m、RT-DETR-L（**训练时**） | 3.10.18 | **2.6.0+cu118** | 11.8 | Ultralytics 8.4.92 |
| conda `mmdetection` | Faster R-CNN、RetinaNet | 3.10.10 | 2.1.0+cu121 | 12.1 | torchvision 0.16.0+cu121、mmcv 2.1.0、mmengine 0.10.7、cuDNN 8.9.2 |
| （YOLO11s/l 用的 `ultralytics` 环境后期状态） | YOLO11s、YOLO11l（2026-08-28~29 训练） | 3.10.18 | 2.5.1+cu124 | 12.4 | Ultralytics 8.4.92 |

### 2.3 环境变更时间线（重要发现）

当前 `ultralytics` conda 环境（torch 2.5.1+cu124）**不是** YOLO11m/RT-DETR 训练时的环境：

1. 2026-07-11 ~ 07-18：YOLO11m 全部 14 个 + RT-DETR 全部 14 个在此环境训练，全部日志头显示 **torch 2.6.0+cu118**；
2. 期间环境被 MMDetection 安装流程污染成 torch 2.1.0+cu121（`/data1/yxli/mmdetection-install.log`）；
3. 2026-07-24 17:22 执行恢复（`/data1/yxli/ultralytics-restore.log`），装回的是 **torch 2.5.1+cu124**（非原版 2.6.0+cu118）；
4. 之后（07-25~08-06）MMDet 实验不受影响（独立环境）；08-28~29 的 YOLO11s/l 在 2.5.1+cu124 下训练。

**结论**：若文章补写 YOLO11m/RT-DETR 的训练环境，应写 torch 2.6.0+cu118，而非当前环境的 2.5.1+cu124；文章 209 行"YOLO11s/l 来自另一运行环境"的说法方向正确，具体差异即 2.6.0+cu118 vs 2.5.1+cu124。文章 607 行"Faster R-CNN 环境未补齐"可以补：与 RetinaNet 完全同一环境。

## 3. Precision / Recall / F1（验证集最佳 F1 阈值协议）

### 3.1 协议与口径

- **阈值协议**：P/R 取 argmax-F1 置信度阈值处的值（阈值在被评测的 val 集上选择，IoU≥0.5 单类匹配），即文章 275 行要求的"验证集选择的最佳 F1 阈值"记录方式；F1 = 2PR/(P+R)；
- **数据来源**：
  - YOLO11m / RT-DETR-L：各 run `results.csv` 中 mAP50-95 最高行的 P、R（与表格 mAP 同行同权重，已验证 mAP 14/14×2 吻合）；
  - Faster R-CNN / RetinaNet：新脚本 `scripts/evaluate_mmdet_pr.py`（2026-09-02 编写并运行）加载 `best_coco_bbox_mAP_epoch_*.pth` 在 val 上重推理，匹配语义复刻 Ultralytics（IoU≥0.5 一对一、按 IoU 降序贪心）；
- **全部 14 个数据集均为 val 划分**（含 Cows2021、Dairy Cow——与表格 mAP 同源；文章 271 行"使用既有 test 划分"的说法与数值实际来源不符，见第 6 节）；
- 两框架微小实现差异：MMDet `test_cfg` 每图最多保留 100 个检测（Ultralytics 为 300），对 argmax-F1 点影响可忽略但非零。

### 3.2 Precision

| 数据集 | YOLO11m | RT-DETR-L | Faster R-CNN | RetinaNet |
|---|--:|--:|--:|--:|
| CBPD_ODD | 0.9898 | 0.9740 | 0.9583 | 0.9681 |
| HCRD | 0.9876 | 0.9906 | 0.9875 | 0.9696 |
| Dairy Cow | 0.9956 | 0.9839 | 0.9989 | 0.9978 |
| CID | 0.9409 | 0.9478 | 0.9569 | 0.9672 |
| Cows2021 | 0.9751 | 0.9577 | 0.9886 | 0.9869 |
| XGain | 0.9796 | 0.9904 | 0.9936 | 0.9529 |
| CImage | 0.9076 | 0.8406 | 0.9123 | 0.8983 |
| NWAFU Cattle Dataset | 0.8763 | 0.8598 | 0.8836 | 0.8742 |
| animals_10 | 0.9027 | 0.8730 | 0.8812 | 0.8759 |
| MooTrack360 | 0.9474 | 0.9447 | 0.9294 | 0.9032 |
| COLO | 0.9175 | 0.9151 | 0.8914 | 0.8865 |
| 8-calves | 0.9628 | 0.9578 | 0.9667 | 0.9140 |
| COCO cow subset | 0.8079 | 0.8183 | 0.8264 | 0.7665 |
| Google Open Images cow subset | 0.8197 | 0.7829 | 0.8131 | 0.7610 |

### 3.3 Recall

| 数据集 | YOLO11m | RT-DETR-L | Faster R-CNN | RetinaNet |
|---|--:|--:|--:|--:|
| CBPD_ODD | 0.9783 | 0.9891 | 1.0000 | 0.9891 |
| HCRD | 0.9746 | 0.9755 | 0.9693 | 0.9785 |
| Dairy Cow | 0.9946 | 0.9826 | 0.9914 | 0.9903 |
| CID | 0.9562 | 0.9610 | 0.9396 | 0.9350 |
| Cows2021 | 0.9796 | 0.9704 | 0.9766 | 0.9717 |
| XGain | 0.9835 | 0.9913 | 0.9554 | 0.9783 |
| CImage | 0.8667 | 0.8833 | 0.8667 | 0.8833 |
| NWAFU Cattle Dataset | 0.8466 | 0.8518 | 0.8215 | 0.7713 |
| animals_10 | 0.7881 | 0.7548 | 0.8027 | 0.6671 |
| MooTrack360 | 0.9206 | 0.9246 | 0.8796 | 0.8330 |
| COLO | 0.8983 | 0.9110 | 0.8646 | 0.7969 |
| 8-calves | 0.9513 | 0.9450 | 0.9418 | 0.8788 |
| COCO cow subset | 0.7101 | 0.7344 | 0.6623 | 0.5775 |
| Google Open Images cow subset | 0.6529 | 0.6338 | 0.6634 | 0.6365 |

### 3.4 F1

| 数据集 | YOLO11m | RT-DETR-L | Faster R-CNN | RetinaNet |
|---|--:|--:|--:|--:|
| CBPD_ODD | 0.9840 | 0.9815 | 0.9787 | 0.9785 |
| HCRD | 0.9810 | 0.9830 | 0.9783 | 0.9740 |
| Dairy Cow | 0.9951 | 0.9832 | 0.9951 | 0.9941 |
| CID | 0.9485 | 0.9544 | 0.9482 | 0.9508 |
| Cows2021 | 0.9773 | 0.9641 | 0.9825 | 0.9792 |
| XGain | 0.9815 | 0.9909 | 0.9741 | 0.9654 |
| CImage | 0.8867 | 0.8614 | 0.8889 | 0.8908 |
| NWAFU Cattle Dataset | 0.8612 | 0.8558 | 0.8514 | 0.8196 |
| animals_10 | 0.8415 | 0.8096 | 0.8401 | 0.7574 |
| MooTrack360 | 0.9338 | 0.9346 | 0.9038 | 0.8667 |
| COLO | 0.9078 | 0.9131 | 0.8778 | 0.8393 |
| 8-calves | 0.9570 | 0.9514 | 0.9541 | 0.8960 |
| COCO cow subset | 0.7558 | 0.7741 | 0.7353 | 0.6587 |
| Google Open Images cow subset | 0.7269 | 0.7005 | 0.7307 | 0.6932 |

### 3.5 最佳轮次 epoch（各 run 的 mAP50-95 最高轮）

| 数据集 | YOLO11m | RT-DETR-L | Faster R-CNN | RetinaNet |
|---|--:|--:|--:|--:|
| CBPD_ODD | 83 | 97 | 50 | 68 |
| HCRD | 90 | 97 | 22 | 80 |
| Dairy Cow | 100 | 97 | 69 | 69 |
| CID | 75 | 52 | 5 | 11 |
| Cows2021 | 30 | 29 | 44 | 63 |
| XGain | 99 | 88 | 76 | 71 |
| CImage | 1 | 3 | 20 | 3 |
| NWAFU Cattle Dataset | 100 | 93 | 8 | 31 |
| animals_10 | 99 | 100 | 8 | 3 |
| MooTrack360 | 99 | 86 | 22 | 28 |
| COLO | 80 | 63 | 7 | 16 |
| 8-calves | 52 | 17 | 92 | 9 |
| COCO cow subset | 90 | 76 | 1 | 5 |
| Google Open Images cow subset | 89 | 99 | 4 | 7 |

说明：Ultralytics 列为 `results.csv` mAP50-95 最高轮（与 4.3 表格 mAP 口径一致，**fitness-best.pt 的轮次可能与此不同**）；MMDet 列为 `save_best='coco/bbox_mAP'` 实际保存的轮次（严格对应加载的 .pth）。MMDet 多个 run 最佳轮次很早（COCO ep=1、Google ep=4、animals_10 ep=8、COLO ep=7 等），是 `save_best` 的既有事实，与文章"CImage 第 1 轮最佳、其高 OOD 表现可能只是更接近原始预训练权重"的观察相互印证。

### 3.6 固定阈值版本

文章 275 行还要求"固定推理阈值"下的记录。28 份 `metrics.json` 中已同时保存固定 score 阈值 0.05 / 0.25 / 0.50 的 P/R/F1（含预测计数），以及平滑 argmax-F1 版本，可按需提取。Ultralytics 侧如需固定阈值需另行用 `model.val(conf=...)` 重评，当前 results.csv 只有 argmax-F1 版本。

## 4. 训练时长记录（墙钟，供"固定训练预算的总训练时间"字段使用）

来源：三个批量脚本的 `summary.log` 的 Starting/Completed 时间戳（串行执行，含加载与验证开销）。

**RT-DETR-L**（2026-07-17~18，批量为 07-17 前单独完成的 7 个无时长记录）：Cows2021 6h57m、Dairy Cow 3h24m、8-calves 51m、COLO 58m、MooTrack360 1h36m、XGain 1h34m、Google 2h37m。

**RetinaNet**（2026-08-04~06，全部 14 个）：8-calves 1h02m、animals_10 3h39m、CBPD_ODD 0h32m、CID 2h42m、CImage 0h36m、COCO 4h39m、COLO 1h15m、Cows2021 11h29m、Dairy Cow 8h18m、Google 7h11m、HCRD 1h19m、MooTrack360 2h25m、NWAFU 2h35m、XGain 2h11m。

**Faster R-CNN**（2026-07-26~28，run_all 串行的 8 个；前 6 个单独启动无完整记录）：COLO 1h24m、Cows2021 16h58m、Dairy Cow 7h40m、Google 6h37m、HCRD 1h34m、MooTrack360 2h32m、NWAFU 4h14m、XGain 约 2h（结束时间未单独记录）。

**P/R/F1 评测耗时**（2026-09-02，evaluate_mmdet_pr.py，A100 串行推理）：Faster R-CNN 14 个约 25 分钟，RetinaNet 14 个约 15 分钟（8-calves 200 图约 20 秒）。

## 5. 证据路径（服务器 `/data1/yxli/CODE/ultralytics/self_mAP/`）

| 内容 | 路径 |
|---|---|
| YOLO11m / RT-DETR 训练参数（14×2 份，含 batch/AMP/optimizer 等） | `outputs/yolo/<数据集>/args.yaml`、`outputs/rtdetr/<数据集>/args.yaml` |
| 逐轮验证曲线（P/R/mAP per epoch） | 同上目录 `results.csv` |
| MMDet 完整生效配置 | `outputs/faster_rcnn/100ep_bs16/<数据集>/effective_config.py`、`outputs/retinanet/100ep_ebs16/<数据集>/effective_config.py` |
| MMDet 训练日志（含环境头、val 指标） | 各 run 目录下 `<时间戳>/<时间戳>.log` 及 `vis_data/scalars.json` |
| 训练脚本 | `scripts/train_rtdetr.py`、`train_faster_rcnn.py`、`train_retinanet.py`、`common.py` |
| 批量脚本（实际运行参数记录） | `scripts/run_rtdetr_all.sh`、`run_faster_rcnn_all.sh`、`run_retinanet_all.sh`、`logs/rtdetr_14/summary.log`、`logs/faster_rcnn_14/100ep_bs16/summary.log`、`logs/retinanet_14/100ep_ebs16/summary.log` |
| P/R/F1 评测（MMDet，2026-09-02 新增） | `scripts/evaluate_mmdet_pr.py`；结果 `outputs/evaluations/faster_rcnn/<数据集>_val/metrics.json`、`outputs/evaluations/retinanet/<数据集>_val/metrics.json`（28 份）；日志 `logs/eval_mmdet_pr.log` |
| 环境头行证据 | `logs/CID_yolo11m_100e_b32_amp.log`（torch 2.6.0+cu118）、`logs/yolo11s_14/*.train.log`（2.5.1+cu124）、各 MMDet `<时间戳>.log`（2.1.0+cu121） |
| 环境变更记录 | `/data1/yxli/ultralytics-restore.log`（07-24 恢复为 2.5.1+cu124）、`/data1/yxli/mmdetection-install.log` |
| 权重文件 | `models/yolo11m.pt`（39M）、`models/rtdetr-l.pt`（64M） |

## 6. 与文章初稿的出入（待作者决定，正文未改）

1. **271 行**："Cows2021、Dairy Cow 使用既有 `test` 划分"——实际 4.3 表格数值（两模型 mAP 均可核验）与 **val** 划分的最佳轮完全一致，test 结果未进入表格；本笔记 P/R/F1 也全部为 val（与表格 mAP 同源同口径）。
2. **275 行**："Precision、Recall、F1 应在统一评估器复核后补充"——现已按 argmax-F1 阈值协议补齐（第 3 节），但评估器仍是两套实现（Ultralytics 内置 vs 自写 MMDet 脚本，语义对齐）；若入文需在表注说明协议与来源。
3. **209 行**：YOLO11m/RT-DETR 训练环境实为 torch 2.6.0+cu118（当前 conda 环境已是 2.5.1+cu124，勿按当前环境填写）；s/l 的 2.5.1+cu124 表述正确。
4. **607 行**："Faster R-CNN 记录尚未补齐环境"——可补：与 RetinaNet 同一环境（Python 3.10.10、torch 2.1.0+cu121、mmcv 2.1.0、mmengine 0.10.7、mmdet 3.3.0）。
5. **两个 0.0001 舍入差**：Dairy Cow YOLO11m 0.8753（表格 0.8754）、CBPD_ODD RT-DETR 0.9509（表格 0.9510），源于文章数值取整方式；统一以 CSV 原值为准即可。
6. **364 行**"最佳 checkpoint 所在 epoch 待汇总"——第 3.5 节已可提供（四架构 × 14 数据集）。
7. MMDet `coco/cow_precision` 键名有误导：实测恒等于单类 `coco/bbox_mAP`（per-class AP），不是 precision，写作时勿引用。
