在 lab 服务器(211.69.141.71 A100 工作站)上的工作主要围绕搭环境 + 跑 Label Studio 标注平台 + 沉淀可复用流程三块,涉及
  yxli 和 youw 两个账号。按主题总结:

  1. 基础环境搭建(yxli 账号,2026-07-09 起)

  ┌──────────────────┬───────────────────────────────────────────────┬──────────────────────────────────────────────┐
  │        项        │                   做了什么                    │                    关键坑                    │
  ├──────────────────┼───────────────────────────────────────────────┼──────────────────────────────────────────────┤
  │ Docker           │ 探查后确认被锁,写了给管理员的解锁指令(二选一) │ user.max_user_namespaces=0 阻断              │
  │                  │                                               │ rootless,yxli 不在 docker 组                 │
  ├──────────────────┼───────────────────────────────────────────────┼──────────────────────────────────────────────┤
  │ Node             │ nvm + Node 20.20.2 unofficial glibc-217 构建  │ 官方 Node 18+ 要 glibc≥2.28,CentOS 7 只有    │
  │                  │                                               │ 2.17                                         │
  ├──────────────────┼───────────────────────────────────────────────┼──────────────────────────────────────────────┤
  │ conda 环境       │ Python 3.10 + LS 1.23.0                       │ pyarrow 22 需 glibc 2.28,pin 到 pyarrow 20   │
  │ label-studio     │                                               │ 绕过                                         │
  ├──────────────────┼───────────────────────────────────────────────┼──────────────────────────────────────────────┤
  │ tmux             │ 装在 conda base,3.3a,加进 .bashrc 登录即用    │ conda solver 太慢,手动下 .conda              │
  │                  │                                               │ 包本地装;3.7b 缺 ncurses 符号退到 3.3a       │
  └──────────────────┴───────────────────────────────────────────────┴──────────────────────────────────────────────┘

  2. Label Studio 标注平台

  yxli 实例(LS 1.23,端口 8080)-- 测试流程用:
  - tmux ls 会话跑 start_ls.sh,配 local-files 两个环境变量
  - 上传 60 张 cow 图 + YOLO 标注,写 yolo_to_ls_predictions.py 转成 LS predictions JSON(52 带框 + 8 空)
  - 踩通 local-files 404 根因:LS 1.23 的 /data/local-files/ 接口校验路径必须属于已注册的 LocalFilesImportStorage
    记录,光设环境变量不够,必须 UI 建 Local Storage 连接
  - 误点 Sync 造了 60 个 $undefined$ 坏副本 -> 用 Django shell + sqlite3 级联清理
  - 最终状态:60 task + 52 predictions,图片正常显示
  - 摸清 LS 1.23 API 走不通(legacy token/Basic auth 被禁),改用 Django shell(label-studio shell --data-dir ...)查改数据

  youw 实例(LS 1.21,端口 8081)-- 正式标注用(6 项目 / 6818 task / 9 账号):
  - 修图片 403(注入 local-files 环境变量,写 Python 启动器 _launch_ls.py 用 start_new_session=True 真脱离,解决 MCP SSH
    通道遇后台 & 断连)
  - 路径迁移:数据实际位置和 DB 里 Local Storage path 不一致,建软链接桥接
  - 确认 9 账号 / 6 项目现状,记录忘密码用 Django shell 重置的方法

  3. SSH 免密配置(2026-07-10)

  - 本地 ~/.ssh/id_rsa.pub 写入 yxli@A100:~/.ssh/authorized_keys(权限 700/600,StrictModes 通过)
  - ~/.ssh/config 给 211.69.141.71 段加 IdentityFile
  - ssh 211.69.141.71 免密验证通过,MCP 密码通道留作备用

  4. 知识沉淀(本地,服务于 lab 服务器操作)

  - 3 个 memory:a100-server-env.md(yxli 实例+系统环境)、youw-a100-account.md(youw
    实例+账号项目)、label-studio-ops.md(横向整合的操作手册)
  - label-studio skill:7 个文件,完全参数化,实例参数从 memory 读;含启动/排障/导入/清理/YOLO 转换/实例清单 6 个
    reference,内联可落地脚本
  - SELF_skills 公共仓库:https://github.com/1034250765/SELF_skills,已推 label-studio skill,本地工作副本
    C:\Users\LYX10\SELF_skills

  核心价值

  不只是"装好了 LS",而是把踩过的坑(local-files 404 根因、Sync 造坏副本、MCP 后台断连、API 走不通改 Django
  shell)都沉淀成了 skill + memory,下次操作任何 LS 实例都能直接复用流程,不用重新踩。