## Node

node是一个js的运行环境

```js
node hello.js 运行代码
```

fs模块可以读取文件内容

```javascript
const fs = require("fs")

fs.readFile()

fs.writeFile()
```

path 路径模块

```javascript
path.join() #多个路径片段进行拼接
path.basename() #用来从路径字符串中，将文件名称解析出来
```

npm与包（第三方模块）